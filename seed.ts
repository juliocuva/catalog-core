import { getPayload } from 'payload'; import configPromise from './payload.config'; async function run() { process.env.DATABASE_URI = 'postgresql://postgres.vidktqijvhchysqakrtg:AlejaUP2020*@aws-0-ca-central-1.pooler.supabase.com:5432/postgres'; process.env.PAYLOAD_SECRET = 'secreto'; const payload = await getPayload({ config: await configPromise }); try { await payload.create({ collection: 'users', data: { email: 'juliocuva@gmail.com', password: 'admin', role: 'administrador' } }); console.log('Admin created!'); } catch(e) { console.error(e); } process.exit(0); } run();
