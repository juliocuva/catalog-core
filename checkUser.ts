import { getPayload } from 'payload'; import configPromise from './payload.config'; async function run() { process.env.DATABASE_URI = 'postgresql://postgres.vidktqijvhchysqakrtg:AlejaUP2020*@aws-0-ca-central-1.pooler.supabase.com:5432/postgres'; process.env.PAYLOAD_SECRET = 'secreto'; const payload = await getPayload({ config: await configPromise }); const users = await payload.find({ collection: 'users' }); console.log('Total users:', users.totalDocs); if (users.totalDocs > 0) { console.log('User 1:', users.docs[0].email); } process.exit(0); } run();
